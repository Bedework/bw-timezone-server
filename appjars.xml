<?xml version="1.0"?>

<!-- ===================================================================
     Fetch or define extra jars for this application
     =================================================================== -->
<project name="extraJars" basedir="." default="">
  <macrodef name="getExtraJars">
    <sequential>
      <delete dir="${org.bedework.temp.extrajars.dir}" />
      <mkdir dir="${org.bedework.temp.extrajars.dir}" />
    	
    	<property name="bedework.home"
    	          location="${project.home}/../bedework" />

    	<!-- Generated by this project -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-tzsvr-cmn" version="${org.bedework.bwtzsvr.version}"
              projecthome="${project.home}" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-tzsvr" version="${org.bedework.bwtzsvr.version}"
              projecthome="${project.home}" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-tzsvr-svc" version="${org.bedework.bwtzsvr.version}"
              projecthome="${project.home}" />

      <!-- ======================= Schema jars ============================= -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-tzschema" version="${org.bedework.bwxml.version}"
              projecthome="${bedework.home}/../bwxml" />

      <!-- ====================== Bedework jars ============================ -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="rpiutil" version="${org.bedework.rpiutil.version}" project="rpiutil"
              projecthome="${bedework.home}/projects/rpiutil" />
      
    	<!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-annotations" version="${org.bedework.calendarapi.version}" project="calendarapi" 
              projecthome="${project.home}/../bedework/projects/calendarapi"/>
    	
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-calfacade" version="${org.bedework.calendarapi.version}" project="calendarapi" 
              projecthome="${project.home}/../bedework/projects/calendarapi" />
              -->

      <!-- Other jars -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-codec" version="1.3"/>
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-httpclient" version="3.0"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-lang" version="2.1"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="ehcache" version="1.6.2" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="ical4j" version="1.0-rc3-SNAPSHOT"/>
      
<!--
    	<getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jta" version="1.1"  />
    	
    	<getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-collections"/>
    	
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-ssl" />
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="exchangews" version="2010"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-logging" />
 -->
      
      <!-- Hibernate jars -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="asm-attrs" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="asm" version="3.2" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="cglib" version="2.1.3" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="hibernate" version="3.3.1.ga" />
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jgroups" version="2.2.8" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jta" version="1.1"  />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-collections"/>
    </sequential>
 </macrodef>
</project>
